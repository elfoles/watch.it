/**
 * Warn view model
 */

var app = app || {};

app.Warn = (function () {
    'use strict';

    var welcomeViewModel = (function () {

        return {

        };

    }());

    return welcomeViewModel;
}());